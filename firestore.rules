rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow anyone to read (so the website works)
      allow read: if true;
      // Allow only logged-in users to write (so only you can change data)
      allow write: if request.auth != null;
    }
  }
}